import numpy as np
from math import e

class simple_exponential(object):
    """a single exponential decay EPSC"""

    def __init__(self, freq, n_pulses, data):

        self.stimulus_times = stimulus_times
        self.max_time = max_time

class two_pool(object):
